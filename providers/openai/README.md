# OpenAI-compatible providers

`sfc.http_providers.OpenAICompatibleProvider` supports OpenAI-compatible chat
completion endpoints, including self-hosted gateways. The core only sees the
provider-neutral request and response contracts.

