# PDF connector

The optional reference implementation lives in `sfc.pdf` and uses `pypdf`
when installed. It emits page elements with source and page provenance. OCR,
regions and visual models belong in a separate adapter and must remain
candidate evidence until admitted by policy.

