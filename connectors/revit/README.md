# Revit connector

Revit integration must remain an adapter. It may extract native parameters,
relationships, schedules, geometry and identity into Project World, but SFC
domain code must not import the Autodesk API. A deployable add-in will be
added here when the public contract is stable.

