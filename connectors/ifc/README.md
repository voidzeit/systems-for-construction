# IFC connector

The dependency-free reference implementation lives in
`packages/sfc-core/src/sfc/ifc.py` so the example works on a clean Python
installation. A production connector can use IfcOpenShell here and must emit
the same Project World contract.

