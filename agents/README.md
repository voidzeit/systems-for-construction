# Agent contracts

Agent manifests declare readable resources, proposal types, limits and
authority. The runtime treats `write: []`, `mayPublish: false` and
`mayApprove: false` as hard boundaries. Model providers can propose findings;
the deterministic evidence and assurance pipeline decides what becomes part of
a canonical run.

