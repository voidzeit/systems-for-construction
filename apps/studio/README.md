# SFC Studio reference UI

`index.html` is a deliberately small read-only viewer for the local API
started with `sfc serve`. It consumes `/project` and `/run`; it contains no
assurance or persistence logic.

