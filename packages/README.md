# Packages

The first executable package is `sfc-core`. Its modules are split by the
intended public boundaries so they can later be published as independently
versioned packages without moving the domain model:

`models` · `assurance` · `authority` · `lifecycle` · `runtime` · `telemetry` ·
`agents` · `providers` · `connector`

